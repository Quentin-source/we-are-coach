<form method="post" action="{{ path('backoffice_comment_delete', {'id': comment_show.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment_show.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

<form method="post" action="{{ path('backoffice_sport_delete', {'id': sport_show.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sport_show.id) }}">
    <button class="btn">Supprimer</button>
</form>

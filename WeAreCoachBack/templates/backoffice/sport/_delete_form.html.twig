<form method="post" action="{{ path('backoffice_sport_delete', {'id': sport_show.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cette donnée?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sport_show.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

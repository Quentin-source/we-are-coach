{% extends 'backoffice/layout.html.twig' %}

{% block title %}Sport{% endblock %}

{% block content %}
    <h1>Sport</h1>

        <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sport_show.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sport_show.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ sport_show.picture }}</td>
            </tr>

             <tr>
                <th>Catégorie</th>
                <td>{{ sport_show.category }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('backoffice_sport_index') }}">back to list</a>

    <a href="{{ path('backoffice_sport_edit', {id:sport_show.id}) }}">edit</a>

    {{ include('backoffice/sport/_delete_form.html.twig') }}
{% endblock %}
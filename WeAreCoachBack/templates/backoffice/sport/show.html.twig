{% extends 'backoffice/layout.html.twig' %}

{% block title %}Sport{% endblock %}

{% block content %}
    <h1>Sport</h1>

        <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sport_show.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ sport_show.name }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ sport_show.picture }}</td>
            </tr>

        </tbody>
    </table>
<p>
    <a href="{{ path('backoffice_sport_index') }}">Retour à la liste</a>
</p>
<p>
    <a href="{{ path('backoffice_sport_edit', {id:sport_show.id}) }}">Modifier</a>
</p>
    {{ include('backoffice/sport/_delete_form.html.twig') }}
{% endblock %}
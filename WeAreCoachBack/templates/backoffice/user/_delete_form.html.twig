<form method="post" action="{{ path('backoffice_user_delete', {'id': user_show.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cette donnée?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_show.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

{% extends 'backoffice/layout.html.twig' %}

{% block title %}Catégorie{% endblock %}

{% block content %}
    <h1>Catégorie</h1>

        <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_show.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_show.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ category_show.picture }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category_show.description }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('backoffice_category_index') }}">back to list</a>

    <a href="{{ path('backoffice_category_edit', {id:category_show.id}) }}">edit</a>

    {{ include('backoffice/category/_delete_form.html.twig') }}
{% endblock %}
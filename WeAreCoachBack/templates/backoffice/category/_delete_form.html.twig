<form method="post" action="{{ path('backoffice_category_delete', {'id': category_show.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cette donnée?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_show.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

{% extends 'backoffice/layout.html.twig' %}

{% block title %}Entrainement{% endblock %}

{% block content %}
    <h1>Entrainement</h1>

        <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ workout_show.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ workout_show.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ workout_show.picture }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ workout_show.level }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ workout_show.description }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('backoffice_workout_index') }}">back to list</a>

    <a href="{{ path('backoffice_workout_edit', {id:workout_show.id}) }}">edit</a>

    {{ include('backoffice/workout/_delete_form.html.twig') }}
{% endblock %}